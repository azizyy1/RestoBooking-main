@page
@model LoginModel
@{
    ViewData["Title"] = "Connexion";
}

<section class="auth-shell">
    <div class="auth-overlay"></div>
    <div class="container">
        <div class="row g-4 align-items-center">
            <div class="col-lg-5">
                <div class="auth-highlight">
                    <div class="auth-badge"><i class="bi bi-stars"></i> Expérience premium</div>
                    <h3>Réservez en toute sérénité</h3>
                    <p>Retrouvez vos réservations, modifiez vos informations et profitez d'un suivi personnalisé
                        dans une interface cohérente avec l'univers RestoBooking.</p>

                    <div>
                        <div class="auth-chip"><i class="bi bi-shield-lock"></i> Sécurité renforcée</div>
                        <div class="auth-chip"><i class="bi bi-lightning-charge"></i> Accès rapide</div>
                        <div class="auth-chip"><i class="bi bi-people"></i> Support dédié</div>
                    </div>

                    <div class="auth-meta">Connexion chiffrée • Notifications en temps réel • Interface sombre élégante
                    </div>
                </div>
            </div>

            <div class="col-lg-5 ms-lg-auto col-md-7">

                <div class="auth-card shadow-lg">
                    <div class="auth-badge">Bienvenue</div>
                    <h1 class="auth-title">Connexion</h1>
                    <p class="auth-subtitle">Accédez à votre compte RestoBooking pour gérer vos réservations.</p>

                    <form id="account" method="post" class="mt-4" asp-route-returnUrl="@Model.ReturnUrl">
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                        <div class="mb-3">
                            <label asp-for="Input.Email" class="form-label">Adresse e-mail</label>
                            <input asp-for="Input.Email" class="form-control auth-input" autocomplete="username"
                                aria-required="true" />
                            <span asp-validation-for="Input.Email" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Input.Password" class="form-label">Mot de passe</label>
                            <div class="input-group">
                                <span class="input-group-text bg-transparent text-light"><i
                                        class="bi bi-shield-lock"></i></span>
                                <input asp-for="Input.Password" class="form-control auth-input"
                                    autocomplete="current-password" aria-required="true" />
                            </div>
                            <span asp-validation-for="Input.Password" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <div class="form-check">
                                <input class="form-check-input" asp-for="Input.RememberMe" />
                                <label class="form-check-label" asp-for="Input.RememberMe">Se souvenir de moi</label>
                            </div>
                            <a class="auth-link" asp-page="./ForgotPassword">Mot de passe oublié ?</a>
                        </div>

                        <button id="login-submit" type="submit" class="btn btn-gold w-100">Se connecter</button>

                        <div class="auth-divider"><span>ou</span></div>
                        <a class="btn btn-outline-gold w-100" asp-page="./Register"
                            asp-route-returnUrl="@Model.ReturnUrl">
                            Créer un compte RestoBooking
                        </a>
                    </form>

                </div>
            </div>
        </div>
    </div>
</section>

@section Styles {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}